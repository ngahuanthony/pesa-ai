import { PublicLayout } from "@/components/layout";
import { Link, useLocation } from "wouter";
import { useAuthRedirect } from "@/hooks/use-auth-redirect";
import { useEffect, useState } from "react";

const maskPhone = (value: string) => value.length > 6 ? value.slice(0, 4) + "***" + value.slice(-3) : value;
export default function LoginPage() { const { me } = useAuthRedirect(); const [, setLocation] = useLocation(); const [phone, setPhone] = useState(""); const [otp, setOtp] = useState(""); const [step, setStep] = useState<"phone" | "otp">("phone"); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); useEffect(() => { if (me?.authenticated && !me?.isAdmin) setLocation("/dashboard"); if (me?.isAdmin) setLocation("/admin"); }, [me, setLocation]); const request = async (event: any) => { event.preventDefault(); setBusy(true); setError(""); try { const response = await fetch("/api/auth/request-otp", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ personalPhone: phone }) }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || "Could not send your code"); setPhone(body.phone || phone); setStep("otp"); } catch (err: any) { setError(err.message); } finally { setBusy(false); } }; const verify = async (event: any) => { event.preventDefault(); setBusy(true); setError(""); try { const response = await fetch("/api/auth/verify-otp", { method: "POST", headers: { "content-type": "application/json" }, credentials: "include", body: JSON.stringify({ personalPhone: phone, code: otp }) }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || "That code is not correct"); setLocation("/dashboard"); } catch (err: any) { setError(err.message); } finally { setBusy(false); } }; return <PublicLayout><div className="flex flex-1 items-center justify-center bg-[#f7faf8] px-4 py-12"><div className="w-full max-w-md rounded-3xl border bg-white p-8 shadow-sm"><h1 className="text-3xl font-extrabold text-foreground">Log in with WhatsApp</h1><p className="mt-2 text-sm text-muted-foreground">No password. We will send a one-time code to your personal WhatsApp.</p>{step === "phone" ? <form onSubmit={request} className="mt-7 space-y-5"><div><label className="text-sm font-semibold">Your Personal WhatsApp</label><input type="tel" value={phone} onChange={(event) => setPhone(event.target.value)} placeholder="0722542810" className="mt-1 w-full rounded-xl border px-4 py-3.5 text-base" required /></div>{error && <div className="rounded-xl bg-destructive/10 p-3 text-sm text-destructive">{error}</div>}<button type="submit" disabled={busy} className="w-full rounded-xl bg-[#0a4a3a] py-3.5 text-sm font-bold text-white disabled:opacity-60">{busy ? "Sending code…" : "Send me a code"}</button></form> : <form onSubmit={verify} className="mt-7 space-y-5"><p className="text-sm text-slate-600">Enter the six-digit code sent to {maskPhone(phone)}.</p><input inputMode="numeric" autoComplete="one-time-code" value={otp} onChange={(event) => setOtp(event.target.value.replace(/\D/g, "").slice(0, 6))} placeholder="000000" className="w-full rounded-xl border px-4 py-3.5 text-center text-2xl tracking-[0.5em]" required />{error && <div className="rounded-xl bg-destructive/10 p-3 text-sm text-destructive">{error}</div>}<button type="submit" disabled={busy || otp.length !== 6} className="w-full rounded-xl bg-[#0a4a3a] py-3.5 text-sm font-bold text-white disabled:opacity-60">{busy ? "Checking code…" : "Log in"}</button><button type="button" onClick={() => { setStep("phone"); setOtp(""); }} className="w-full text-sm font-semibold text-primary underline">Use a different number</button></form>}<p className="mt-6 text-center text-sm text-muted-foreground">New here? <Link href="/signup" className="font-semibold text-primary underline">Create your shop</Link></p></div></div></PublicLayout>; }
